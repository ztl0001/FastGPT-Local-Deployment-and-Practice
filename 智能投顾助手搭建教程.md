# 从零搭建"智能投顾助手"：部署、配模型、建知识库、搭工作流（小白完整版）

> 本教程是《Windows 本地部署 FastGPT 完全教程（小白版）》的续篇，记录了从"FastGPT 已经部署好"到"智能投顾助手可以被别人使用"的完整过程，全部步骤均在实际环境中验证通过。
>
> 环境基线：Windows 11 + Docker Desktop + FastGPT v4.17.0（docker-compose 部署，模型网关为 aiproxy）

---

## 目录

1. [本项目要做什么](#一本项目要做什么)
2. [全局架构：先看懂再动手](#二全局架构先看懂再动手)
3. [第一步：配置模型（最常见的坑）](#三第一步配置模型最常见的坑)
4. [第二步：创建金融知识库](#四第二步创建金融知识库)
5. [第三步：搭建工作流](#五第三步搭建工作流)
6. [第四步：调试与验证清单](#六第四步调试与验证清单)
7. [第五步：分享给其他人使用](#七第五步分享给其他人使用)
8. [踩坑实录与经验总结](#八踩坑实录与经验总结)

---

## 一、本项目要做什么

我们要在本地 FastGPT 上搭建一个"智能投顾助手"，它根据用户输入自动判断意图并走不同分支：

| 用户意图 | 处理分支 | 用到的节点 |
|---|---|---|
| 金融概念咨询 | 检索金融知识库后回答 | 问题分类 → 知识库搜索 → AI 对话（投资知识教育专员） |
| 股票/行情查询 | 调用 MCP 实时工具 | 问题分类 → 工具调用（挂 3 个工具集） |
| 投资诊断 | 先做风险问卷再出报告 | 问题分类 → 表单输入 → AI 对话（风险评估分析师） |
| 通用寒暄 | 直接回复 | 问题分类 → AI 对话（通用咨询专员） |

---

## 二、全局架构：先看懂再动手

FastGPT 本身**不带任何模型**，它通过一个叫 aiproxy（AI Proxy）的模型网关去调用大模型。整条链路是：

```
用户 → FastGPT 工作流 → aiproxy 模型网关 → 外部模型 API（DeepSeek / 硅基流动等）
                ↓
          知识库（pgvector 向量检索，依赖 Embedding 模型）
```

三个角色记住就能少踩 90% 的坑：

- **对话模型（LLM）**：负责聊天、分类、写报告，如 `deepseek-flash`。
- **索引/向量模型（Embedding）**：把知识库文本变成向量用于检索，如 `BAAI/bge-m3`。**DeepSeek 官方不提供 Embedding 模型**，所以建知识库前必须另配一个。
- **模型网关（aiproxy）**：统一管理模型渠道和 Key，FastGPT 从它这里拿模型列表。

---

## 三、第一步：配置模型（最常见的坑）

### 3.1 注册一个提供 Embedding 模型的服务

推荐 [硅基流动 SiliconFlow](https://siliconflow.cn)：注册即送额度，`BAAI/bge-m3` 是**免费**的中文 Embedding 模型，一个 API Key 可以调用平台上所有模型。

登录后进入「API 密钥」页面，新建并复制 Key。

### 3.2 在 aiproxy 里添加渠道

登录 FastGPT（root）后，进入「账户」→「模型提供商/渠道管理」（v4.17 由 aiproxy 接管）：

1. 新建渠道：
   - 名称：`siliconflow`
   - 类型：OpenAI 兼容
   - **Base URL：`https://api.siliconflow.cn/v1`**（结尾必须有 `/v1`，少了会报上游 404！）
   - Key：粘贴你的硅基流动 Key
   - 模型：填 `BAAI/bge-m3`
2. 保存后用渠道的"测试"按钮验证连通性。

### 3.3 把 bge-m3 声明为 Embedding 模型

渠道里只声明了"我能调这个模型"，还需要告诉 aiproxy **这个模型是什么类型**。在 aiproxy 的模型配置里给 `BAAI/bge-m3` 添加类型为 `Embeddings`（向量）的模型配置，并设置最大上下文（8192）。

做完后重启 `fastgpt-app` 容器，让 FastGPT 重新拉取模型列表：

```powershell
docker restart fastgpt-app
```

**验证**：FastGPT 里新建知识库时，索引模型应能看到 `BAAI/bge-m3`。如果界面死活不显示，检查 FastGPT 数据库 `ai_models` 集合里是否真的有这条记录（v4.17 的模型列表存在 MongoDB 中，不是实时从 aiproxy 读取的，模型同步有时需要手动触发或重启）。

### 3.4 对照检查清单

- [ ] aiproxy 渠道 `siliconflow` 已创建，Base URL 带 `/v1`
- [ ] 用渠道测试按钮验证 `BAAI/bge-m3` 调用成功
- [ ] aiproxy 中该模型类型为 Embeddings
- [ ] 已重启 fastgpt-app
- [ ] FastGPT 界面能看到索引模型

---

## 四、第二步：创建金融知识库

### 4.1 新建知识库

1. 顶部导航进入「**知识库**」→「新建」→ 选「**通用知识库**」（不要选文件夹）。
2. 名称：`金融投资知识库`。
3. 索引模型：应默认选中 `BAAI/bge-m3`，其他保持默认。

### 4.2 准备文档并导入

支持 txt / docx / pdf / md 等。文档质量决定回答质量，建议覆盖：

- 基础概念（股票、债券、基金、ETF、市盈率、市净率、股息率……）
- 投资理论（资产配置、分散投资、复利、定投、均值回归……）
- 风险知识（风险类型、承受能力影响因素、控制方法）
- 案例分析（正反案例各几个，方便模型"讲故事"）

> 本项目配套的《投资基础知识文档.md》就是按这个结构写的，可直接导入。

导入时分段设置保持默认；等待状态变为「已完成」。

### 4.3 必做：搜索测试

知识库里点「搜索测试」，输入"什么是市盈率"，能返回相关片段才算建好。**这一步不过关，不要进入工作流环节**，否则排查问题时知识库和流程互相甩锅。

---

## 五、第三步：搭建工作流

### 5.1 新建工作流应用

工作台 → 创建应用 → 选「**工作流**」类型（不要选简易对话）→ 命名"智能投顾助手"。

### 5.2 节点清单（共 10 个节点）
工作流排布如下图：
![alt text](<屏幕截图 2026-09-24 012323.png>)

```
流程开始
  └→ 问题分类（4 个分类项）
        ├→ 咨询金融知识   → 知识库搜索 → 投资知识教育专员（AI 对话）
        ├→ 风险评估       → 表单输入 → 风险评估分析师（AI 对话）
        ├→ 查询市场       → 工具调用（挂 3 个工具集节点）
        └→ 其他内容       → 通用咨询专员（AI 对话）
```

### 5.3 各节点配置要点

**问题分类节点**（自带节点，不用手搓提示词）

添加 4 个分类项（名称即分类依据）：

1. 咨询金融知识
2. 风险评估
3. 查询或分析今日市场，基金，个股的情况
4. 其他内容

背景提示词：`理解用户输入，并根据用户输入进行分类并选择分支`。分类项和**连线出口一一对应**，连错分支就串线，保存后务必核对。

**知识库搜索**

- 选择刚建的金融知识库
- 相似度 0.4 起步
- **最大引用数量设 6 左右**（默认可能很大，会挤占上下文、让回答啰嗦）

**投资知识教育专员（AI 对话）**

系统提示词：

```
## 角色
你是投资知识教育专员，负责向用户讲解金融投资知识。

## 要求
1. 主要依据知识库检索结果回答，使用通俗易懂的语言。
2. 回答结构：先一句话解释概念，再展开要点，最后给一个例子或应用场景。
3. 如果知识库检索结果为空或与问题无关，就基于常识谨慎回答，并说明该内容仅供参考，不构成投资建议。
```

**关键动作**：在「引用」输入框里选择知识库搜索节点的 `quoteQA` 输出。**不连引用，知识库等于白建**。

**表单输入**：添加 5 个字段（年龄-数字、投资经验-下拉、月收入水平-下拉、风险承受能力-下拉、投资目标-文本），全部设为必填。

**风险评估分析师（AI 对话）**

系统提示词：

```
## 角色
你是风险评估分析师。用户已完成风险评估问卷，请根据问卷结果生成结构化的投资策略分析报告。

## 输出结构
1. 用户画像小结
2. 风险承受能力评估（保守/稳健/平衡/成长/进取）
3. 建议的资产配置比例（如股票/债券/现金百分比及理由）
4. 市场环境提示（基于通用知识，不编造实时数据）
5. 风险提示与免责声明

注意：报告仅供参考，不构成投资建议。
```

用户问题输入：引用表单输入节点的 `formInputResult`（完整问卷结果）。

**工具调用（市场新闻情报专员）**

挂载你的 MCP 工具集（如"今日投资市场实时数据""股票、基金、投资组合分析""可视化图表"）：

我们可以在魔搭社区（ModelScope）的 MCP 市场中找到"可视化图表 MCP Server"。该服务基于 TypeScript 开发，兼容 MCP 协议，提供了生成面积图、柱状图、饼图等多种图表的能力，能够将枯燥的数据转化为直观的可视化结果。
![alt text](<屏幕截图 2026-09-24 094833.png>)

阿里云百炼平台也提供了丰富的官方 MCP 服务。在 MCP 管理页面中，我们可以找到"今日投资-金融实..."和"且慢"等金融类 MCP 服务，以及股票实时行情查询、万相-视频生成等工具。将这些服务添加到 FastGPT 的 MCP 工具库后，智能体便能在对话中按需调用它们。
![alt text](<屏幕截图 2026-09-24 094859.png>)

系统提示词：

```
## 角色
你是市场新闻情报专员，负责查询或分析实时市场数据并回答用户问题。

## 工作方式
1. 根据用户问题判断需要调用哪个工具。
2. 获取工具返回的数据后，用中文清晰总结，标注数据时间和来源。
3. 数据获取失败时如实告知，不要编造行情数据。
```

**通用咨询专员（AI 对话）**：处理寒暄闲聊，提示词一句"回复用户的日常问题"即可。

### 5.4 开场白（在对话配置里设置）

```
您好！我是您的智能投顾助手 👋 我可以为您提供以下服务：

1️⃣ 金融知识咨询 —— 解答股票、基金、债券、ETF 等投资概念与理论
2️⃣ 市场实时查询 —— 查询今日行情、个股基金数据、生成可视化图表
3️⃣ 投资诊断分析 —— 通过风险评估问卷，为您量身生成投资策略分析报告

💡 试试对我说："帮我做个投资诊断" 或 "什么是定投"

⚠️ 温馨提示：内容仅供学习参考，不构成投资建议，投资有风险，决策需谨慎。
```

---

## 六、第四步：调试与验证清单

保存工作流后，在调试窗口用 4 句话各测一遍：

| 测试输入 | 预期 |
|---|---|
| 什么是市盈率 | 走知识库分支，回答基于文档 |
| 你好 | 走通用寒暄 |
| 帮我做个投资诊断 | 弹出风险评估问卷，提交后生成报告 |
| 今天股市行情怎么样 | 触发 MCP 工具调用 |

**自查 4 个高频错误**（都可以在保存后的画布上看出来）：

1. 知识库搜索节点后面**没连下游节点** → 检索结果没人用。
2. AI 对话节点**没引用 quoteQA** → 搜到了也不用。
3. 表单输入节点**字段为空或后面没连线** → 问卷数据丢失。
4. 分类项与连线出口**接错分支** → 回答串台。

---

## 七、第五步：分享给其他人使用

三种方式按场景选：

| 方式 | 适用场景 | 要点 |
|---|---|---|
| 局域网分享 | 同一 WiFi/办公室 | 创建应用的"免登录分享链接"，把 `localhost` 换成本机内网 IP（如 `http://192.168.x.x:3000/...`），Windows 防火墙放行 3000 端口 |
| 团队成员账号 | 长期协作 | 管理员邀请成员，把应用共享给对应成员 |
| 公网访问 | 异地/客户 | 内网穿透（ngrok/frp/花生壳）或部署到云服务器；注意本机关机服务即停，正式使用建议上云 |

---

## 八、踩坑实录与经验总结

本项目实际踩过的坑，按出现顺序：

1. **"没有可用的索引模型"** → DeepSeek 不提供 Embedding 模型，必须另配（硅基流动 bge-m3 免费）。
2. **Embedding 调用上游 404** → aiproxy 渠道 Base URL 少了 `/v1`。
3. **配完渠道 FastGPT 还是看不到模型** → FastGPT 的模型注册表存在自己的 MongoDB（`ai_models` 集合），不是实时读 aiproxy；需要同步/重启，必要时手动补记录。
4. **知识库搜索结果无人使用** → 搜索节点后忘连 AI 对话、AI 对话忘引用 quoteQA。
5. **表单输入字段为空** → 节点加了但问卷字段没设计，等于没配。

**三条通用心法**：

- 搭任何分支前，先让**最小可运行版本**跑通（分类 → 一条简单分支），再逐条加分支，随时能在预览里看到效果。
- 每配完一个环节就做一次**独立验证**（模型测试、知识库搜索测试、单分支调试），不要等全搭完再排错。
- 调试时优先看**连线**（数据流）再看**提示词**，大部分"模型不听话"其实是节点没连对或引用没选。

---

## 附录：完整检查清单

部署（详见前篇教程）：

- [ ] Docker + FastGPT v4.17.0 全部容器 Running/Healthy
- [ ] root 能登录 http://localhost:3000

模型：

- [ ] 硅基流动渠道已建（Base URL 带 /v1）
- [ ] bge-m3 已声明为 Embedding 类型
- [ ] FastGPT 能看到索引模型并设为默认

知识库：

- [ ] 金融投资知识库已建，bge-m3 为索引模型
- [ ] 文档导入完成、状态"已完成"
- [ ] 搜索测试通过

工作流：

- [ ] 4 条分支连线正确（分类项 ↔ 出口）
- [ ] 教育专员已引用 quoteQA
- [ ] 表单 5 字段齐全、后接分析师节点
- [ ] 工具调用挂载 MCP 工具且配提示词
- [ ] 引用上限调整为 6 左右
- [ ] 4 句话冒烟测试全部通过

分享：

- [ ] 开场白已配置
- [ ] 分享链接已创建（局域网 IP 或公网地址）
